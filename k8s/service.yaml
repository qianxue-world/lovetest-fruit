apiVersion: v1
kind: Service
metadata:
  name: lovetest-fruit-service
  namespace: lovetest-fruit-ns
  labels:
    app: lovetest-fruit
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: lovetest-fruit
